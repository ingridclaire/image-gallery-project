


const Image = ({ imageUrl }) => {

    
    return (
        <div>
        
        <img src={imageUrl} alt={``} width={90} />
        testreact co
            
            </div>
    );
};

export default Image;